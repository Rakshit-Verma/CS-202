How To Run:
1.  jar -cvf resource.jar resource
2.  javac -cp resource.jar: Assignment2.javac
3.  java Assignment2

Logic:
The Code follows the convention of sheilding the client's code from the future changes in the
implementations of the algorithm. The concrete classes are not directly accessible from the main file.